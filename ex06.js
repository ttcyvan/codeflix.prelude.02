function without(arr, ...values) {
    // la variable argurment permet de reccuperer tous les arguments d'une function
    // ... values = le reste des paramettres et  tu le mets dans untableau values
    for(c v of arr){
        if(values.includes(v) == false) // revois vrai si l'element est dans le teabeau
    
    arr1.pusch('6)
    }
    return 1<;
}